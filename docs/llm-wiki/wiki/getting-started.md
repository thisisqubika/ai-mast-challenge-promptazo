# Getting started — ai-mast-challenge-promptazo

_Generated by the AI Agentic Framework. Source-of-truth for "how to run this project" — derived from your `Makefile` / `Justfile` / `Taskfile` / setup scripts (Tier 1), README "Getting Started" sections (Tier 2), per-service package-manager scripts (Tier 3), and CI hints (Tier 4)._

## From the project README

_Reproduced verbatim from the README — the original wording is authoritative for any quirks the wrapper / package-manager tables below may miss._

### How to Run It Locally

_Source: `README.md` § How to Run It Locally_

> **Backend**
>
> ```bash
> cd fanfest/backend
> pip install -r requirements.txt
> cp .env.example .env        # fill in ANTHROPIC_API_KEY and Google credentials
> uvicorn app.main:app --reload
> # API available at http://localhost:8000
> ```
>
> **Frontend**
>
> ```bash
> cd fanfest/frontend
> python -m http.server 8080
> # Open http://localhost:8080 in your browser
> ```
>
> **Environment variables required** (see `fanfest/backend/.env.example`):
> - `ANTHROPIC_API_KEY` — for the AI recap feature
> - Google OAuth credentials — for Calendar, Maps, and Drive integrations
>

## (No commands discovered)

Phase 1 analyzers found no Makefile / Justfile / Taskfile / setup script / package-manager scripts / CI commands in this project. Add a wrapper (`Makefile` with documented targets) or a README "Getting Started" section, then re-run `/initialize-project`.
